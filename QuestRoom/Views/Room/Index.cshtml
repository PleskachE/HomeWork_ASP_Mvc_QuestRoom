@using QuestRoom.Models;

@model RoomsAndPictersContainer
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    @foreach(var item in Model.Rooms)
    {
        <div style="display:inline-block; width:200px; height:100px; margin:10px; border-color:red; border-width:2px;">
            <img src=@Model.Pictures.ToList().Find(x => x.RoomId ==item.RoomId).Path />
            <p>Квест: <a href="Room/Result/@item.RoomId">@item.Name</a></p>
        </div>
    }
</body>
</html>
